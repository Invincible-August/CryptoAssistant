"""
Lightweight Charts compatibility mapping package.

This directory holds the backend-side mapping utilities that transform
internal indicator/chart data into the shape expected by `lightweight-charts`.
"""

