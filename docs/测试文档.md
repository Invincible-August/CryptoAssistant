# 测试文档

## 自动化测试

### 运行单元测试
```bash
cd backend
pytest tests/ -v
```

### 运行测试覆盖率
```bash
pytest tests/ --cov=app --cov-report=html
```

## 手动测试步骤

### 1. 系统部署测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1.1 | 复制.env.example为.env | 配置文件创建成功 |
| 1.2 | 启动PostgreSQL和Redis | 数据库和缓存服务正常运行 |
| 1.3 | pip install -r requirements.txt | 所有依赖安装成功 |
| 1.4 | 运行create_admin.py | 管理员用户创建成功 |
| 1.5 | 运行seed_demo_data.py | 示例数据导入成功 |
| 1.6 | uvicorn启动后端 | 访问/health返回ok |
| 1.7 | npm run dev启动前端 | 前端页面正常展示 |

### 2. 认证测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 2.1 | POST /api/v1/auth/login 正确凭据 | 返回JWT令牌 |
| 2.2 | POST /api/v1/auth/login 错误密码 | 返回401错误 |
| 2.3 | GET /api/v1/auth/me 携带令牌 | 返回用户信息 |
| 2.4 | 前端登录页输入admin/admin123456 | 跳转到仪表盘 |

### 3. 实时监控测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.1 | GET /api/v1/monitor/watches | 返回监控列表（包含种子数据的3个交易对） |
| 3.2 | POST /api/v1/monitor/watches 添加新交易对 | 返回新监控记录 |

### 4. 技术指标测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 4.1 | GET /api/v1/indicators/ | 返回6个已注册指标 |
| 4.2 | GET /api/v1/indicators/rsi/meta | 返回RSI元数据 |
| 4.3 | POST /api/v1/indicators/calculate | 返回指标计算结果 |

### 5. 自定义因子测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 5.1 | GET /api/v1/factors/ | 返回6个已注册因子 |
| 5.2 | main_force_cost_zone因子元数据 | source=human |

### 6. AI模块测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 6.1 | MODULE_AI_ENABLED=false时调用 | 返回403模块未启用 |
| 6.2 | 启用AI后调用/ai/analyze | 返回AI分析结果 |
| 6.3 | POST /ai/feedback | 返回反馈已记录 |
| 6.4 | POST /ai/learn-placeholder | 返回占位信息 |

### 7. 回测测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 7.1 | POST /api/v1/backtest/run（有种子数据） | 返回回测结果和绩效指标 |
| 7.2 | GET /api/v1/backtest/tasks | 返回回测任务列表 |
| 7.3 | GET /api/v1/backtest/tasks/{id}/trades | 返回交易明细 |

### 8. 执行辅助测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 8.1 | POST /execution/orders（模拟模式） | 返回模拟订单结果 |
| 8.2 | DELETE /execution/orders/{id} | 订单状态变为cancelled |

### 9. 模块开关测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 9.1 | GET /config/modules | 返回所有模块配置 |
| 9.2 | PUT /config/modules/ai 启用 | AI模块启用成功 |
| 9.3 | PUT /config/modules/ai 禁用 | AI模块禁用成功 |

### 10. 权限测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 10.1 | 普通用户访问/config/modules | 返回403 |
| 10.2 | 管理员访问/config/modules | 返回正常数据 |
| 10.3 | 无令牌访问受保护接口 | 返回401 |

### 11. 异常场景测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 11.1 | 数据库连接断开后请求 | 返回500但不崩溃 |
| 11.2 | Redis断开后请求 | 系统正常运行（不使用缓存） |
| 11.3 | 请求不存在的指标key | 返回404 |
| 11.4 | 回测数据不足 | 返回明确的错误提示 |
